import { CakiCurrencyPipe } from './caki-currency.pipe';

describe('CakiCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CakiCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
